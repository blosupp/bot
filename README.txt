✅ README.md (готов к использованию)
markdown
Копировать
Редактировать
# 🤖 Telegram AI Бот (на GPT-4o)

Этот бот предназначен для генерации постов с помощью GPT-4o и автоматической публикации их в Telegram-каналы пользователей. Имеет гибкую систему ролей (superadmin → admin → user) и управление через интерфейс Telegram.

---

## 🚀 Возможности

- ✍️ Генерация постов (текст / изображение + подпись)
- 📢 Автопубликация в каналы, привязанные к пользователю
- ⚙️ Интерактивная админ-панель через /доступ
- 🔐 Ролевой доступ (`user`, `admin`, `superadmin`)
- 🧠 Настройки памяти GPT (remember, history_limit)
- 🧩 Работа с базой SQLite (`access.db`, `dialogues.db`)
- 🌐 Используется OpenAI GPT-4o через API

---

## ⚙️ Установка

```bash
git clone https://github.com/your_username/your_repo.git
cd your_repo
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
📦 Переменные окружения (.env)
Создайте файл .env в корне проекта:

ini
Копировать
Редактировать
BOT_TOKEN=xxx:yyy
OPENAI_API_KEY=sk-...
CHANNEL_ID=-1001234567890
ADMIN_ID=123456789
📌 Хотя CHANNEL_ID и ADMIN_ID указаны, всё управление делается через базу данных (access.db).

🧠 GPT-модель
Используется модель gpt-4o через OpenAI API.

🗂 Структура проекта
python
Копировать
Редактировать
bot/
├── handlers/
│   ├── start.py
│   ├── help.py
│   ├── text.py         # генерация по тексту
│   ├── photo.py        # генерация по фото + caption
│   └── admin_panel.py  # панель управления
├── middleware/
│   └── access_control.py  # автодобавление пользователей
├── services/
│   └── openai_service.py  # обращение к GPT
├── db.py
├── access.py              # работа с users/channels
├── config.py
├── main.py
🔐 Роли
Роль	Возможности
superadmin	управляет пользователями и их каналами
admin	может генерировать и публиковать посты
user	(ограничено, можно отключить генерацию)

Все роли и каналы хранятся в базе access.db.

💬 Команды
Команда	Доступ	Описание
/start	все	Запуск бота
/help	все	Все команды с учётом роли
/myid	все	Узнать свой Telegram ID
/доступ	admin+	Интерактивная панель (добавление/удаление)
/add_admin	superadmin	Назначить админа
/list_users	superadmin	Просмотреть всех пользователей и роли

📌 Панель /доступ включает:

➕ Добавить пользователя

❌ Удалить пользователя

📡 Добавить канал

🗑 Удалить канал

📋 Мои каналы

📤 Публикация постов
📄 Напишите любой текст → будет сгенерирован и отправлен в ваш канал.

📷 Отправьте фото с подписью → будет сгенерирован caption и опубликован.

Канал должен быть добавлен через кнопку в /доступ и начинаться с -100.

💾 Хранилище
access.db — пользователи, роли, каналы

dialogues.db — история сообщений для GPT

🧪 Запуск (в PyCharm или вручную)
bash
Копировать
Редактировать
python main.py
При запуске автоматически создаются все таблицы и добавляется первый superadmin, если не существует.

📌 TODO (дальнейшее развитие)
⏰ Отложенные посты (планировщик)

🧾 Шаблоны / категории постов

📊 Аналитика по генерации

📁 Интеграция с Google Sheets или CRM

🧑‍💻 Автор
Проект ведётся и тестируется вручную. Для доработки или автоматизации — см. [handlers/admin_panel.py].